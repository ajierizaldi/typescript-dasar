{
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "name": "typescript-dasar",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "rmdir /s /q build && mkdir build && tsc",
    "ts": "rmdir /s /q build && mkdir build && tsc -w",
    "dev": "nodemon ./build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.3.3",
    "nodemon": "^2.0.20"
  }
}
